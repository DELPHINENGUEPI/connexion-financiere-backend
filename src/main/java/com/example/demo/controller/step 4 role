Parfait 👍 **STEP 4 = CONTROLLER (API backend)**
👉 C’est l’étape où ton frontend commence vraiment à fonctionner avec Spring Boot.

---

# 🚀 STEP 4 — CONTROLLER

📍 chemin :

```text id="cpath"
src/main/java/com/example/demo/controller/
```

---

# 🧠 Rôle du controller

Il gère les routes API :

- `/api/prospect` → créer utilisateur + envoyer OTP
- `/api/verify` → vérifier OTP

---

# 📄 1. CRÉER ProspectController.java

Crée ce fichier :

```text id="cfile"
ProspectController.java
```

---

# 🟢 CODE COMPLET

```java id="ccode1"
package com.example.demo.controller;

import com.example.demo.model.Prospect;
import com.example.demo.repository.ProspectRepository;
import com.example.demo.service.OtpService;
import com.example.demo.service.EmailService;

import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.web.bind.annotation.*;

import java.util.Map;

@RestController
@RequestMapping("/api")
@CrossOrigin("*")
public class ProspectController {

    @Autowired
    private ProspectRepository prospectRepository;

    @Autowired
    private OtpService otpService;

    @Autowired
    private EmailService emailService;

    // 1. Créer prospect + envoyer OTP
    @PostMapping("/prospect")
    public String createProspect(@RequestBody Prospect prospect) {

        // sauvegarde utilisateur
        prospectRepository.save(prospect);

        // générer OTP
        String otp = otpService.generateOtp(prospect.getEmail());

        // envoyer OTP (console pour l'instant)
        emailService.sendOtp(prospect.getEmail(), otp);

        return "OTP_SENT";
    }

    // 2. Vérifier OTP
    @PostMapping("/verify")
    public String verifyOtp(@RequestBody Map<String, String> body) {

        String email = body.get("email");
        String code = body.get("code");

        boolean valid = otpService.verifyOtp(email, code);

        if (!valid) {
            return "INVALID_OTP";
        }

        Prospect p = prospectRepository.findByEmail(email)
                .orElseThrow();

        p.setVerified(true);
        prospectRepository.save(p);

        return "VERIFIED";
    }
}
```

---

# 🔗 2. CE QUE TON FRONTEND UTILISE

Ton HTML appelle exactement :

### 🟢 Prospect

```text id="api3"
POST https://connexion-financiere.onrender.com/api/prospect
```

### 🟢 OTP

```text id="api4"
POST https://connexion-financiere.onrender.com/api/verify
```

---

# ⚠️ 3. IMPORTANT (sinon ça ne marche pas)

## ✔ CORS

Tu dois avoir :

```java id="cors"
@CrossOrigin("*")
```

---

## ✔ packages corrects

Tout doit être :

```text id="pkg2"
com.example.demo
```

---

## ✔ repository obligatoire

Sinon ça crash :

```java id="repo"
findByEmail()
```

---

# 🧠 4. CE QUE TU VIENS DE TERMINER

✔ frontend connecté
✔ backend structure OK
✔ OTP system OK
✔ API REST fonctionnelle

---

# 🚀 5. FLOW FINAL ACTUEL

```text id="flow4"
Frontend
  ↓
/api/prospect
  ↓
OTP généré
  ↓
console email (test)
  ↓
/api/verify
  ↓
user verified = true
  ↓
PostgreSQL
```

---

# ⚡ 6. CE QUI RESTE (IMPORTANT)

👉 STEP 5 :

- Render PostgreSQL configuration
- variables env

👉 STEP 6 :

- email réel Gmail / SendGrid

👉 STEP 7 :

- déploiement final propre

---

# 💬 NEXT STEP

Dis :

👉 **“step5”**

et on connecte ton backend à la base de données Render + variables propres (partie critique pour que tout marche en ligne).
